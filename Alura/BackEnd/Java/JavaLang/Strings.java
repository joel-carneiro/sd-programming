/**
 * .toUpperCase() = Transforma para maíscula.
 * .toLowerCase() = Transforma para minscula.
 * .charAt(int example) = Retorna o char no index example
 * .indexOf(char example) = Retorna o index do char example
 * .subString(1) = Retorna a String sem os caracteres anteriores a 1
 * .isEmpty() = String vazia: true or false
 * .trim() = Retira todos os espaços do inicio e fim
 *
 */

public class Strings {
    public static void main(String[] args) {
        int number = 123;

        print(number);
    }

    static void print(Object value) {
        System.out.println(value);
    }
}
