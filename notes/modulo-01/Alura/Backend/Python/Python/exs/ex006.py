# Crie um algoritmo que leia um número e mostre o seu dobro, triplo e raiz quadrada.

import math as mt

number = float(input('? '))

print('dobro: ', number * 2)
print('triplo: ', number * 3)
print('raiz quadrada: ', mt.sqrt(number))